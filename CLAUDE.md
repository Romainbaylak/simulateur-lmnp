# Instructions permanentes — simulateur-lmnp

## Git & déploiement

- **Toujours travailler et pusher directement sur `main`** — jamais sur une branche intermédiaire (pas de `claude/...`, pas de PR, pas de Preview Vercel).
- Chaque commit va directement en production.
- `git config user.email noreply@anthropic.com && git config user.name Claude` avant chaque commit.
- Format de commit : message clair en français ou anglais + `Co-Authored-By: Claude <noreply@anthropic.com>` en pied.
